// ignore_for_file: constant_identifier_names

enum ViewState { IDLE, BUSY, ERROR, UPDATE }

enum CustomRadioButtonEnum { checked, notChecked }

enum CustomRadioBtnEnum { checked, notChecked }

enum UrlType { IMAGE, VIDEO, UNKNOWN }